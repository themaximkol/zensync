# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
MANIFEST

# Virtual environments
.venv/
venv/
env/

# pytest
.pytest_cache/
.coverage
htmlcov/

# Editor
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# zensync runtime artifacts (snapshots, staging, local state)
*.tar.zst
*.tar.zst.tmp

# Profile-side temporary directories written during apply
.zensync-incoming/
.zensync-backup/
.zensync.lock

# Local agent state and config (user-specific, not for version control)
state.json
client.toml

# Outbox (queued pushes when hub is unreachable)
outbox/

# Pending conflicts awaiting resolution
pending/
